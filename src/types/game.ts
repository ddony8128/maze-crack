export type BuilderTool = 'START' | 'GOAL' | 'WALL';

export type {
  Difficulty,
  Direction,
  GameMode,
  MazeSpec as Maze,
  PlayerId as Player,
  Position,
  PublicGameState,
  WallKey,
} from '@/engine/types';
